# Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Absolute origin used to build the Google OAuth redirect URL (e.g.
# https://civiclens.example.com). Falls back to the request's Host header
# in local dev if unset - set this explicitly in production.
NEXT_PUBLIC_SITE_URL=
RESEND_API_KEY=
# Supabase project settings > Database
# Use the direct connection string (port 5432) for local dev and migrations.
# For serverless/edge deployments, use the transaction pooler (port 6543)
# connection string ins
#GROQ API
NEXT_PUBLIC_GROQ_API=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
